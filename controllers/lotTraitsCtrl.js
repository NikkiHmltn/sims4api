// getAllLotTraits, getPackLotTraits, getSpecificLotTrait
const db = require('../models')

const getAllLotTraits = (req, res) => {
    try {
        
    } catch (err) {

    }
}

const getPackLotTraits = (req, res) => {
    try {
        
    } catch (err) {
        
    }
}

const getSpecificLotTrait = (req, res) => {
    try {
        
    } catch (err) {
        
    }
}

module.exports = {
    getAllLotTraits, 
    getPackLotTraits, 
    getSpecificLotTrait
}